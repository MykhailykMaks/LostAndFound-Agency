<Window x:Class="LostAndFoundAgency.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Бюро знахідок - Головне меню" Height="600" Width="900"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#2C3E50" Offset="0.0"/>
                    <GradientStop Color="#3498DB" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="40">
                <TextBlock Text="🔍" FontSize="80" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <TextBlock Text="МІСЬКЕ БЮРО ЗНАХІДОК" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                <TextBlock Text="Єдина система пошуку та реєстрації втрачених речей." FontSize="16" Foreground="#ECF0F1" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Background="White">
            <StackPanel VerticalAlignment="Center" Margin="50">
                <TextBlock Text="ЛАСКАВО ПРОСИМО" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Оберіть дію для початку роботи" FontSize="14" Foreground="#7F8C8D" HorizontalAlignment="Center" Margin="0,0,0,40"/>

                <Button x:Name="btnLogin" Content="УВІЙТИ В СИСТЕМУ" Click="btnLogin_Click" Padding="15" Background="#3498DB" Foreground="White" FontWeight="Bold" FontSize="14" Cursor="Hand" BorderThickness="0" Margin="0,0,0,20">
                    <Button.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button x:Name="btnRegister" Content="СТВОРИТИ АКАУНТ" Click="btnRegister_Click" Padding="15" Background="White" Foreground="#3498DB" FontWeight="Bold" FontSize="14" Cursor="Hand" BorderThickness="2" BorderBrush="#3498DB">
                    <Button.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>